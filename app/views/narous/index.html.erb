<% provide :title, "登録済みweb小説一覧" %>
<div class="container">
  <h3 class="mt-4">登録済みweb小説一覧</h1>
  <div class="search-box mt-4">
    <%= form_with url: narous_path, method: :get, local: true do |f|%>
      <div class="input-group">
        <%= f.text_field :keyword, placeholder: "小説のタイトルまたは著者を入力してください", class: "form-control", id: "narou-keyword" %>
        <span class="input-group-btn">
          <%= f.submit '検索', name: nil, class: "form-control" %>
        </span>
      </div>
    <% end %>
  </div>
</div>
<div class="album py-5">
  <div class="container">
    <div class="row">
      <% @narous.each do |narou| %>
        <div class="col-md-4 mt-4 narou-box">
          <div class="card mb-2 shadow-sm h-100">
            <div class="card-body">
              <h5 class="card-title"><%= narou.title %></h5>
              <p class="card-text"><%= narou.writer %></p>
              <div class="d-flex justify-content-between align-items-center mt-2">
                <button type="button" class="btn btn-sm btn-outline-secondary"><%= link_to "レビューする", narou %></button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
